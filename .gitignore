node_modules/
dist/
build/
coverage/
.cache/
.turbo/


# Local engram memory dumps (chunks contain personal observations)
.engram/

# Claude Code skill registry cache (machine-specific)
.atl/

# Claude Code local skills (machine/personal; agents are versioned intentionally)
.claude/skills/

# Superpowers brainstorm companion (local session artifacts)
.superpowers/

# Secrets and local env files
.env
.env.*
!.env.example
*.local

# Logs
logs/
*.log
npm-debug.log*
pnpm-debug.log*

# OS junk
.DS_Store
Thumbs.db

# IDE
.vscode/
!.vscode/extensions.json
!.vscode/settings.json.example
.idea/

# TypeScript / Vite incremental state
*.tsbuildinfo
.vite/

# Go build artifacts (compiled binaries from `make build` / `go build`)
/engram-explorer
/engram-explorer.exe
/engram-explorer.exe~
/seed-demo
/seed-demo.exe
/parity-diff
/parity-diff.exe
/bin/
*.exe~

# Demo database (generated by `make seed-demo` or `go run ./cmd/seed-demo`)
/demo/
# Ignore the real frontend build output but keep the placeholder index.html
# so `go build ./...` always compiles without running `make build` first.
internal/web/dist/*
!internal/web/dist/index.html
*.test
