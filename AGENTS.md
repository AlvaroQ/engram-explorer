# Agent Guide — engram-explorer

## Project Skills

Project-specific skills live under `.claude/skills/{name}/SKILL.md`. Invoke them via the `Skill` tool when their trigger matches.

| Skill | Trigger | Purpose |
|-------|---------|---------|
| `graph3d-viz` | 3D node graph, force graph, R3F nodes, edge tunnels, particle/light flow, graph performance | Build a heavy instanced 3D node-graph in React Three Fiber with zoom-gated tunnel light animation. Stack: React 18 + Vite + R3F. |
